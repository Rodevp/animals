import { CardContainer } from './styles';
import { CardHeader } from '../header-card';

const CardComponent = () => {
  return (
    <CardContainer>
      <CardHeader />
    </CardContainer>
  );
};

export default CardComponent;
